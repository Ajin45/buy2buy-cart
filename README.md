# 🚀 EC2 Build & Deploy Pipeline (Packer + Terraform + GitHub Actions)

This repository automates creating an AMI using Packer and deploying an EC2 instance using Terraform, integrated with GitHub Actions workflow.

📘 Workflow Summary

Packer → builds AMI

Terraform → deploys EC2

GitHub Actions → automates build + deploy pipeline


This repository automates creating an AMI using Packer and deploying an EC2 instance using Terraform, integrated with GitHub Actions workflow.

📘 Workflow Summary

Packer → builds AMI

Terraform → deploys EC2

GitHub Actions → automates build + deploy pipeline

